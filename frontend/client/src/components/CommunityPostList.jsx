"use client"; import CommunityPostCard from "./CommunityPostCard"; export default function CommunityPostsList({ posts = [], currentUserId, onEdit, onDelete, }) { return ( <div className="w-full space-y-8"> {posts.length > 0 ? ( posts.map((post) => ( <CommunityPostCard key={post.id} post={post} currentUserId={currentUserId} onEdit={() => onEdit(post.id)} onDelete={() => onDelete(post.id)} /> )) ) : ( <div className="bg-[#0F2A21] rounded-3xl p-12 text-center"> <p className="text-gray-400 text-lg"> No community posts yet. Be the first to share your story! </p> </div> )} </div> ); }